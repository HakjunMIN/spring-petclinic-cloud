# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/petclinic***REMOVED***
spring.datasource.username=${MYSQL_USER:petclinic***REMOVED***
spring.datasource.password=${MYSQL_PASS:petclinic***REMOVED***
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
