---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: customers-service
  name: customers-service
  namespace: spring-petclinic
***REMOVED***
  replicas: 1
  selector:
    matchLabels:
      app: customers-service
  strategy: {***REMOVED***
  template:
    metadata:
      labels:
        app: customers-service
    ***REMOVED***
      containers:
      - image: ${REPOSITORY_PREFIX***REMOVED***/spring-petclinic-cloud-customers-service:latest
        name: customers-service
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            port: 8080
            path: /actuator/health/liveness
          initialDelaySeconds: 90
          periodSeconds: 5
        readinessProbe:
          httpGet:
            port: 8080
            path: /actuator/health/readiness
          initialDelaySeconds: 15
        lifecycle:
          preStop:
            exec:
              command: ["sh", "-c", "sleep 10"]
        resources:
          limits:
            memory: 1Gi
          requests:
            cpu: 2000m
            memory: 1Gi
      ***REMOVED***
        - name: SPRING_PROFILES_ACTIVE
          value: kubernetes
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://customers-db-mysql.spring-petclinic.svc.cluster.local:3306/service_instance_db?queryInterceptors=brave.mysql8.TracingQueryInterceptor&exceptionInterceptors=brave.mysql8.TracingExceptionInterceptor&zipkinServiceName=customers-db
      ***REMOVED***
          value: root
      ***REMOVED***
      ***REMOVED***
       ***REMOVED***
               name: customers-db-mysql
               key: mysql-root-password
        - name: MANAGEMENT_METRICS_EXPORT_WAVEFRONT_URI
          value: proxy://wavefront-proxy.spring-petclinic.svc.cluster.local:2878
    ***REMOVED***
          value: c91aa53f-77a3-4009-b9f0-2c8a1f513596s  
        ports:
        - containerPort: 8080
        resources: {***REMOVED***
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred
status: {***REMOVED***
