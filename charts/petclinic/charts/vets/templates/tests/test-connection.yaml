apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visits.fullname" . ***REMOVED******REMOVED***-test-connection"
  labels:
    {{- include "visits.labels" . | nindent 4 ***REMOVED******REMOVED***
  annotations:
    "helm.sh/hook": test
***REMOVED***
  containers:
    - name: wget
    ***REMOVED*** busybox
      command: ['wget']
      args: ['{{ include "visits.fullname" . ***REMOVED******REMOVED***:{{ .Values.service.port ***REMOVED******REMOVED***']
  restartPolicy: Never
